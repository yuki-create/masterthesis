・バグ?
入力信号は確かにsin波だが、ばねを全て切断しても外力による運動が不規則。y負方向へも運動。
->関数Fの戻り値　return ( sum - force_x[idx1] - gamma1 * array3[idx1] ) / m[idx1];　はy方向の計算も共通なので、
  y方向にもforce_xがかかっている。
!x方向の計算にのみ外力項を入れるためにFxとFyに分けたところ、x方向にのみ振動するようになった。
減衰項を消してもやたらと遅いが？
!外力がsin波なだけであって、質点の運動がsin波であるわけではない。外力の大きさは運動の速さに反映されている様子。

・差分
ばねを全て切断した時、in_p[]配列に指定したindexのみがx軸方向に運動することを確認。
ばねの長さを保持する配列l[M],　長さを求める関数getSpringLength()を実装。
